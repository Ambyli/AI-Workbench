[project]
name = "kokoro-app"
version = "0.1.0"
description = "Kokoro-82M TTS inference server"
requires-python = ">=3.11"
dependencies = [
    "kokoro>=0.9.2",
    "soundfile",
    "fastapi",
    "uvicorn",
    "misaki[en]",
]
