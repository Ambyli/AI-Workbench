{
  "info": {
    "name": "supabase",
    "description": "Self-hosted Supabase gateway endpoints (ai/supabase). Unlike the other collections in this repo, these go DIRECT to the Envoy API gateway on the box — Supabase is not registered as a LiteLLM pass-through or MCP server, so there is no {{litellm}} base URL and no virtual key here.\n\nSet the collection variables before use:\n  supabase          the gateway origin, e.g. http://localhost:8022 (PORT_SUPABASE_API in .env). Must match SUPABASE_PUBLIC_URL when called from a browser — Envoy CORS is an exact match.\n  anonKey           SUPABASE_ANON_KEY from .env (HS256 JWT, role=anon)\n  serviceRoleKey    SUPABASE_SERVICE_ROLE_KEY from .env (role=service_role — server-side only, never in client code)\n  dashboardUser     SUPABASE_DASHBOARD_USERNAME (default: supabase)\n  dashboardPassword SUPABASE_DASHBOARD_PASSWORD\n  table             a table name, for the PostgREST row-read request\n\nThis deployment runs in LEGACY HS256 API key mode: authenticate with the `apikey` header (and `Authorization: Bearer` where a user/role token is wanted). The newer sb_publishable_… / ES256 keys are a documented follow-up and are deliberately empty — see ai/supabase/SUPABASE.md § Asymmetric keys.\n\nThese are the same requests as SUPABASE.md § Verifying. Full operator guide: ai/supabase/SUPABASE.md.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
    "_postman_id": "supabase-collection"
  },
  "variable": [
    {
      "key": "supabase",
      "value": "http://localhost:8022",
      "description": "Envoy API gateway origin. Host port is PORT_SUPABASE_API in .env (default 8022). Use the box's LAN IP when calling from another machine."
    },
    {
      "key": "anonKey",
      "value": "",
      "description": "SUPABASE_ANON_KEY from .env — the HS256 JWT with role=anon. Generated by ai/supabase/bin/generate_keys.py."
    },
    {
      "key": "serviceRoleKey",
      "value": "",
      "description": "SUPABASE_SERVICE_ROLE_KEY from .env — role=service_role, bypasses row-level security. Server-side only."
    },
    {
      "key": "dashboardUser",
      "value": "supabase",
      "description": "SUPABASE_DASHBOARD_USERNAME — the basic-auth user Envoy requires in front of Studio."
    },
    {
      "key": "dashboardPassword",
      "value": "",
      "description": "SUPABASE_DASHBOARD_PASSWORD — the basic-auth password Envoy requires in front of Studio."
    },
    {
      "key": "table",
      "value": "",
      "description": "A table in one of SUPABASE_PGRST_DB_SCHEMAS, for the PostgREST row-read request. This instance ships with no application tables, so create one in Studio first."
    }
  ],
  "item": [
    {
      "name": "GET /auth/v1/health",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{supabase}}/auth/v1/health",
          "host": ["{{supabase}}"],
          "path": ["auth", "v1", "health"]
        },
        "description": "GoTrue liveness. Returns { version, name, description } and 200 once auth has connected to Postgres. No API key required — this is the cheapest proof that the gateway is routing and the auth container is up.\n\nErrors: 503 from Envoy while supabase-auth is still starting (it waits on supabase-db being healthy, which can be ~90s on first boot). Connection refused means the gateway itself is down — check `make logs supabase supabase-api`.\n\nSee ai/supabase/SUPABASE.md § Verifying."
      },
      "response": []
    },
    {
      "name": "GET /rest/v1/ — PostgREST root (OpenAPI)",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "apikey",
            "value": "{{anonKey}}",
            "description": "Legacy HS256 anon key. Envoy checks it; PostgREST derives the `anon` DB role from it."
          }
        ],
        "url": {
          "raw": "{{supabase}}/rest/v1/",
          "host": ["{{supabase}}"],
          "path": ["rest", "v1", ""]
        },
        "description": "PostgREST's OpenAPI description of every table and view exposed by SUPABASE_PGRST_DB_SCHEMAS (default public,graphql_public). Returns a JSON OpenAPI document; on a fresh instance `definitions` is essentially empty because no application tables exist.\n\nErrors: 401 with a JWSError / 'No API key found in request' when the `apikey` header is missing or was signed with a different SUPABASE_JWT_SECRET than the one PostgREST is running with. 500 from PostgREST usually means SUPABASE_JWT_JWKS was set non-empty, flipping it to asymmetric verification — see SUPABASE.md § Asymmetric keys.\n\nSee ai/supabase/SUPABASE.md § Access."
      },
      "response": []
    },
    {
      "name": "GET /rest/v1/:table — read rows",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "apikey",
            "value": "{{anonKey}}"
          },
          {
            "key": "Authorization",
            "value": "Bearer {{anonKey}}",
            "description": "PostgREST reads the role from this token. Swap for {{serviceRoleKey}} to bypass row-level security."
          }
        ],
        "url": {
          "raw": "{{supabase}}/rest/v1/:table?select=*&limit=10",
          "host": ["{{supabase}}"],
          "path": ["rest", "v1", ":table"],
          "query": [
            { "key": "select", "value": "*" },
            { "key": "limit", "value": "10" }
          ],
          "variable": [
            {
              "key": "table",
              "value": "{{table}}",
              "description": "Table or view name in an exposed schema. Set the `table` collection variable, or type one here."
            }
          ]
        },
        "description": "Read rows from one table through PostgREST. Returns a JSON array, capped by SUPABASE_PGRST_DB_MAX_ROWS (default 1000) regardless of `limit`.\n\nErrors: 404 'relation does not exist' when the table is outside SUPABASE_PGRST_DB_SCHEMAS. An EMPTY array where rows clearly exist is the row-level-security signature — the anon role is being filtered by an RLS policy; retry with {{serviceRoleKey}} to confirm. (The same trap is why the Trino reader role carries BYPASSRLS — see SUPABASE.md § Trino federation.)\n\nSee ai/supabase/SUPABASE.md § Access."
      },
      "response": []
    },
    {
      "name": "GET /storage/v1/status",
      "request": {
        "method": "GET",
        "header": [],
        "url": {
          "raw": "{{supabase}}/storage/v1/status",
          "host": ["{{supabase}}"],
          "path": ["storage", "v1", "status"]
        },
        "description": "Storage API liveness. Returns 200 with an empty body once storage has migrated its schema and reached PostgREST and imgproxy.\n\nErrors: 503 while storage is still starting — in this compose file it waits for supabase-db, rest AND imgproxy to be healthy (tighter than upstream, which only waits for them to have started). A persistent 5xx usually means the storage schema migration failed; check `make logs supabase storage`.\n\nSee ai/supabase/SUPABASE.md § Verifying."
      },
      "response": []
    },
    {
      "name": "GET /functions/v1/hello",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Authorization",
            "value": "Bearer {{anonKey}}",
            "description": "Only required when SUPABASE_FUNCTIONS_VERIFY_JWT=true. It defaults to false, so this header is optional today."
          }
        ],
        "url": {
          "raw": "{{supabase}}/functions/v1/hello",
          "host": ["{{supabase}}"],
          "path": ["functions", "v1", "hello"]
        },
        "description": "Invoke the `hello` edge function — ai/supabase/volumes/functions/hello/index.ts, a plain Deno.serve handler. Returns { message, method, timestamp }.\n\nThis function is OURS, not upstream's sample: upstream's imports @supabase/server and authenticates with the new publishable/secret keys, which 401 in this deployment's legacy HS256 mode.\n\nErrors: 401 when SUPABASE_FUNCTIONS_VERIFY_JWT=true and the Bearer token is missing or invalid (the setting applies to ALL functions — there is no per-function override). 503 while supabase-functions is starting; it waits on the gateway being healthy.\n\nSee ai/supabase/SUPABASE.md § Vendored config."
      },
      "response": []
    },
    {
      "name": "GET /realtime/v1/api/tenants/realtime-dev/health",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Authorization",
            "value": "Bearer {{anonKey}}",
            "description": "Realtime's tenant API requires a token signed with SUPABASE_JWT_SECRET."
          }
        ],
        "url": {
          "raw": "{{supabase}}/realtime/v1/api/tenants/realtime-dev/health",
          "host": ["{{supabase}}"],
          "path": ["realtime", "v1", "api", "tenants", "realtime-dev", "health"]
        },
        "description": "Realtime tenant health — the same check the container's own healthcheck runs. Returns { data: { healthy, db_connected, connected_cluster } }.\n\n`realtime-dev` is the tenant name Realtime derives from the hostname it is reached at, which is why the Envoy cluster config addresses the container as realtime-dev.supabase-realtime and the compose file adds that as a network alias on supabase_net.\n\nErrors: 401 when the Bearer token was signed with a different SUPABASE_JWT_SECRET. `healthy: false` with `db_connected: false` after a secret rotation is the one-shot-secret trap — Realtime stores the tenant's jwt_secret encrypted under SUPABASE_REALTIME_DB_ENC_KEY. See SUPABASE.md § Secrets that are one-shot."
      },
      "response": []
    },
    {
      "name": "GET / — Studio dashboard (basic auth)",
      "request": {
        "method": "GET",
        "auth": {
          "type": "basic",
          "basic": [
            { "key": "username", "value": "{{dashboardUser}}", "type": "string" },
            { "key": "password", "value": "{{dashboardPassword}}", "type": "string" }
          ]
        },
        "header": [],
        "url": {
          "raw": "{{supabase}}/",
          "host": ["{{supabase}}"],
          "path": [""]
        },
        "description": "The Supabase Studio dashboard. Envoy puts HTTP basic auth in front of it using SUPABASE_DASHBOARD_USERNAME / SUPABASE_DASHBOARD_PASSWORD (the entrypoint SHA1-hashes the password into the Envoy user list at container start). Returns Studio's HTML.\n\nOpen this one in a browser rather than Postman. If Studio loads but its panels fail with CORS errors, SUPABASE_PUBLIC_URL does not exactly match the origin you typed — Envoy CORS is an exact match, and a trailing slash is enough to break it.\n\nErrors: 401 on wrong credentials. 503 while supabase-studio is still starting (the gateway waits for it to be healthy, and Studio in turn waits for postgres-meta).\n\nSee ai/supabase/SUPABASE.md § Access."
      },
      "response": []
    }
  ]
}
