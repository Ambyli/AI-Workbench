connector.name=postgresql
# roofix-db is on ai_shared, reachable by service DNS.
connection-url=jdbc:postgresql://roofix-db:5432/${ENV:ROOFIX_DB_NAME}
connection-user=${ENV:ROOFIX_DB_USER}
connection-password=${ENV:ROOFIX_DB_PASSWORD}
