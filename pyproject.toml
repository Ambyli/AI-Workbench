[tool.uv.workspace]
members = [
    "shared/common",
    "widget",
    "ai/roofix",
    "ai/interceptor",
    "ai/classifier",
    "ai/sandbox/runner",
    "ai/trino/mcp",
]

[tool.uv.sources]
common = { workspace = true }
