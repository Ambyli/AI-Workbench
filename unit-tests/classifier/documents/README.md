# Classifier document fixtures

Hand-testing material for the classifier's document support: one file per
input kind and per interesting failure mode, with the criteria to send and the
result to expect. Everything here is generated by
[`make_fixtures.py`](make_fixtures.py) — edit the recipe and re-run rather than
hand-editing a binary:

```bash
uv run --package classifier python unit-tests/classifier/documents/make_fixtures.py
```

The Postman collection mirrors this folder: `ai/classifier/classifier.postman_collection.json`
has a **Documents** subfolder with one ready-to-run `POST /assess` per fixture,
each already pointing at the file path below and carrying the same criteria.
Import the collection, set the `litellm` and `virtual master key` variables,
and click Send.

Total size: ~950 KB (the two PNGs dominate; they are JPEG round-tripped so PNG
can compress the sensor noise).

---

## Files

| File | Kind | Text layer | Page images | What it exercises |
|---|---|---|---|---|
| [`invoice_native.pdf`](invoice_native.pdf) | pdf | native, 2 pages | 2 renders @ `CLASSIFIER_PDF_RENDER_DPI` | Native PDF text extraction, multi-page `cv` (worst page wins), all four text-match modes |
| [`invoice_scanned.pdf`](invoice_scanned.pdf) | pdf | **none** — OCR required | 2 renders | The scan path: `ocr=auto` detects the missing text layer and recognises both pages |
| [`contract.txt`](contract.txt) | txt | native | none | Plain text: `cv` criteria SKIP, the LLM call is text-only |
| [`proposal.docx`](proposal.docx) | docx | native (paragraphs **and** table cells) | none | Document-order extraction; a table row is searchable as `System Size \| 8.4 kW` |
| [`photo_of_letter.png`](photo_of_letter.png) | image | **none** — OCR required | 1 | A badly photographed letter (3° skew, brightness gradient, mild blur, JPEG noise) that OCR still reads |
| [`photo_of_letter_blurry.png`](photo_of_letter_blurry.png) | image | **none** — OCR mostly fails | 1 | Sharpness FAIL, partial OCR, and `depends_on` skipping the LLM criterion |
| [`unsupported_legacy.doc`](unsupported_legacy.doc) | — | — | — | The OLE2 rejection: HTTP 400 telling the caller to convert to .docx |

---

## Suggested criteria and expected results

Results below are what the pipeline actually produced against these fixtures
with `CLASSIFIER_OCR_ENGINE=rapidocr` and the vision model stubbed at score 8
(so the `llm` rows say "8 PASS" — a real Muse Glimmer call will differ, the
`cv` and `text` rows will not).

### `invoice_native.pdf`

```json
[
  {"name": "Invoice #INV-2026-0042", "type": "text", "match": "contains", "weight": 3.0},
  {"name": "Notice to Owner",        "type": "text", "match": "fuzzy",    "weight": 3.0},
  {"name": "Payment Terms: Net 30",  "type": "text", "match": "exact",    "weight": 2.0},
  {"name": "total amount",           "type": "text", "match": "regex", "pattern": "\\$[\\d,]+\\.\\d{2}"},
  {"name": "lien waiver attached",   "type": "text", "match": "fuzzy", "pattern": "Lien Waiver Release"},
  {"name": "sharpness",              "type": "cv"},
  {"name": "document legibility",    "type": "llm",  "hint": "quality"}
]
```

| Criterion | Expected | Why |
|---|---|---|
| `Invoice #INV-2026-0042` | **PASS** 10, `method: text` | Exact string is in the page-1 text layer |
| `Notice to Owner` | **PASS** 10 (ratio 0.86) | The document says `NOTICE TO OWNER`; fuzzy clears the 0.85 default |
| `Payment Terms: Net 30` | **PASS** 10 | Whole-line match on **page 2** — `detail.pages` is `[1]` |
| `total amount` | **PASS** 10, `count: 3` | Regex finds `$1,200.00`, `$3,650.00`, `$4,850.00` |
| `lien waiver attached` | **FAIL** 1 (best ratio ≈ 0.47) | Not in the document; below the 0.5 partial-credit floor, so no points |
| `sharpness` | **PASS** 10, `detail` ends "(worst of 2 pages — page N)" | Rendered text pages are crisp |
| `document legibility` | LLM-scored | One page image (page 0) plus the full extracted text go to the model |
| **Overall** | PASS (9) | `document_info.ocr.ran` is **false** — a native text layer needs no OCR |

### `invoice_scanned.pdf`

```json
[
  {"name": "Notice to Owner",        "type": "text", "match": "fuzzy", "fuzzy_threshold": 0.8, "weight": 3.0},
  {"name": "Invoice #INV-2026-0042", "type": "text", "match": "fuzzy", "fuzzy_threshold": 0.75},
  {"name": "Payment Terms: Net 30",  "type": "text", "match": "fuzzy", "fuzzy_threshold": 0.8},
  {"name": "sharpness",              "type": "cv"},
  {"name": "document legibility",    "type": "llm",  "hint": "quality"}
]
```

| Criterion | Expected | Why |
|---|---|---|
| `Notice to Owner` | **PASS** 10 (ratio ≈ 0.86) | OCR read both pages; `document_info.pages[*].text_source` is `ocr` |
| `Invoice #INV-2026-0042` | **PASS** 10 (ratio ≈ 0.98) | OCR drops the space in `Invoice#INV-…` — exactly why fuzzy exists here |
| `Payment Terms: Net 30` | **PASS** 10 | Page 2, recognised cleanly |
| `sharpness` | **PASS** ~8 | JPEG scan is softer than a native render but well above the threshold |
| **Overall** | PASS (9), `ocr.ran: true`, `pages_recognised: 2`, page confidence ≈ 0.99 | |

Re-send with `ocr=never` to watch every text criterion flip to **FAIL** 1 with
`"No text available for this document…"` — the fastest way to confirm OCR is
what is doing the work.

### `contract.txt`

```json
[
  {"name": "Limited Warranty",  "type": "text", "weight": 3.0},
  {"name": "contract date",     "type": "text", "match": "regex", "pattern": "\\d{4}-\\d{2}-\\d{2}"},
  {"name": "sharpness",         "type": "cv"},
  {"name": "mentions a warranty period", "type": "llm", "hint": "presence"}
]
```

| Criterion | Expected | Why |
|---|---|---|
| `Limited Warranty` | **PASS** 10, `count: 3` | Appears in the heading and twice in clause 2 |
| `contract date` | **PASS** 10 | Regex matches `2026-03-14` |
| `sharpness` | **SKIPPED**, `method: skipped` | A .txt has no page images; excluded from the weighted score entirely |
| `mentions a warranty period` | LLM-scored, **text-only prompt** | `document_info.llm_image_page` is `null` |

### `proposal.docx`

```json
[
  {"name": "Roofix Proposal",       "type": "text", "weight": 2.0},
  {"name": "System Size | 8.4 kW",  "type": "text", "match": "contains", "weight": 3.0},
  {"name": "has faces",             "type": "cv"},
  {"name": "states the system size","type": "llm",  "hint": "presence"}
]
```

| Criterion | Expected | Why |
|---|---|---|
| `Roofix Proposal` | **PASS** 10, `count: 2` | Title heading plus the Terms paragraph |
| `System Size \| 8.4 kW` | **PASS** 10 | Table rows are flattened to `cell \| cell`, so the row is one searchable line |
| `has faces` | **SKIPPED** | .docx has no page images |
| `states the system size` | LLM-scored, text-only prompt | Paragraphs and table cells arrive in document order |

### `photo_of_letter.png`

```json
[
  {"name": "Notice to Owner", "type": "text", "match": "fuzzy", "fuzzy_threshold": 0.8, "weight": 3.0},
  {"name": "case number",     "type": "text", "match": "regex", "pattern": "CASE-\\d{5}", "weight": 2.0},
  {"name": "sharpness",       "type": "cv",   "weight": 2.0},
  {"name": "proper exposure", "type": "cv"},
  {"name": "document legibility", "type": "llm", "hint": "quality"}
]
```

| Criterion | Expected | Why |
|---|---|---|
| `Notice to Owner` | **PASS** 10 (ratio ≈ 0.88) | OCR reads the skewed, unevenly lit page (confidence ≈ 0.99) |
| `case number` | **PASS** 10 | `CASE-77813` survives recognition |
| `sharpness` | **PASS** ~10 (variance ≈ 330) | Mild blur only |
| `proper exposure` | **PASS** ~9 (mean ≈ 203) | The brightness gradient stays inside the accepted range |
| **Overall** | PASS (10), `ocr.ran: true`, `pages_recognised: 1` | |

### `photo_of_letter_blurry.png`

```json
[
  {"name": "Notice to Owner", "type": "text", "match": "fuzzy", "fuzzy_threshold": 0.8, "weight": 3.0},
  {"name": "case number",     "type": "text", "match": "regex", "pattern": "CASE-\\d{5}", "weight": 2.0},
  {"name": "sharpness",       "type": "cv",   "weight": 2.0},
  {"name": "document legibility", "type": "llm", "hint": "quality", "depends_on": "sharpness"}
]
```

| Criterion | Expected | Why |
|---|---|---|
| `sharpness` | **FAIL** 1 (variance ≈ 6) | Heavily out of focus |
| `Notice to Owner` | **PASS** 10 | Only the two large headings survive OCR — and that is one of them |
| `case number` | **FAIL** 1 | Body text is unreadable; OCR returns ~32 characters in total |
| `document legibility` | **SKIPPED** | `depends_on: sharpness` did not PASS, so the LLM is never called |
| **Overall** | MARGINAL (5) | |

### `unsupported_legacy.doc`

Any criteria. Expected: **HTTP 400** at submit time with

```
Legacy .doc (OLE2 compound) files are not supported. Convert the file to .docx
(Word: File → Save As → .docx, or `libreoffice --convert-to docx`) and upload
that instead.
```

---

## curl examples

Through the LiteLLM pass-through (`{{litellm}}` = `http://localhost:4001`, key
from the LiteLLM Admin UI). Both endpoints are async: the POST returns a
`job_id`, then you poll.

```bash
LITELLM=http://localhost:4001
KEY=sk-your-litellm-virtual-key
DOCS=unit-tests/classifier/documents

# 1 — native PDF, all four text-match modes
curl -s $LITELLM/v1/classifier/assess \
  -H "Authorization: Bearer $KEY" \
  -F "file=@$DOCS/invoice_native.pdf" \
  -F 'criteria=[
    {"name":"Invoice #INV-2026-0042","type":"text","match":"contains","weight":3.0},
    {"name":"Notice to Owner","type":"text","match":"fuzzy","weight":3.0},
    {"name":"Payment Terms: Net 30","type":"text","match":"exact","weight":2.0},
    {"name":"total amount","type":"text","match":"regex","pattern":"\\$[\\d,]+\\.\\d{2}"},
    {"name":"sharpness","type":"cv"},
    {"name":"document legibility","type":"llm","hint":"quality"}
  ]'
# → {"job_id":"abc123","phase":"pending"}

curl -s $LITELLM/v1/classifier/jobs/abc123 -H "Authorization: Bearer $KEY"

# 2 — scanned PDF: OCR does the work
curl -s $LITELLM/v1/classifier/assess \
  -H "Authorization: Bearer $KEY" \
  -F "file=@$DOCS/invoice_scanned.pdf" \
  -F "ocr=auto" \
  -F 'criteria=[
    {"name":"Notice to Owner","type":"text","match":"fuzzy","fuzzy_threshold":0.8,"weight":3.0},
    {"name":"document legibility","type":"llm","hint":"quality"}
  ]'

# 3 — the same file with OCR off: every text criterion FAILs with a reason
curl -s $LITELLM/v1/classifier/assess \
  -H "Authorization: Bearer $KEY" \
  -F "file=@$DOCS/invoice_scanned.pdf" \
  -F "ocr=never" \
  -F 'criteria=[{"name":"Notice to Owner","type":"text","match":"fuzzy"}]'

# 4 — .docx: cv criteria SKIP, the LLM prompt is text-only
curl -s $LITELLM/v1/classifier/assess \
  -H "Authorization: Bearer $KEY" \
  -F "file=@$DOCS/proposal.docx" \
  -F 'criteria=[
    {"name":"System Size | 8.4 kW","type":"text","weight":3.0},
    {"name":"has faces","type":"cv"},
    {"name":"states the system size","type":"llm","hint":"presence"}
  ]'

# 5 — legacy .doc: rejected immediately
curl -s $LITELLM/v1/classifier/assess \
  -H "Authorization: Bearer $KEY" \
  -F "file=@$DOCS/unsupported_legacy.doc" \
  -F 'criteria=[{"name":"Notice to Owner","type":"text"}]'
# → 400 "Legacy .doc (OLE2 compound) files are not supported…"

# What can this deployment actually handle right now?
curl -s $LITELLM/v1/classifier/document-kinds -H "Authorization: Bearer $KEY"
```

The legacy field name still works — `-F "image=@…"` is accepted anywhere
`-F "file=@…"` is, so existing scripts keep running.

---

## Notes

* **`sharpness` on a rendered PDF page measures the render, not the original.**
  A 150 dpi render of crisp text has a very high Laplacian variance; a JPEG
  scan has a much lower one. Use `cv` criteria to judge photos, and the text
  layer to judge documents.
* **`ocr.ran: false` is normal** for `invoice_native.pdf`, `contract.txt`, and
  `proposal.docx`: `ocr=auto` only loads the OCR models when a page actually
  has an image and no text.
* **Regenerating changes the bytes only if the recipe changed** — the noise
  generator is seeded (`np.random.default_rng(20260921)`).
* See [`ai/classifier/API.md`](../../../ai/classifier/API.md) for the full
  request/response reference, and `GET /document-kinds` for the live capability
  list of a running container.
