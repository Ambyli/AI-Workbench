# cdp_interceptor

> **Moved.** `cdp_interceptor` is now part of the repo-root `common` package. See its co-located README:
>
> **[`shared/common/src/common/cdp_interceptor/README.md`](../shared/common/src/common/cdp_interceptor/README.md)**
>
> Import path is now `common.cdp_interceptor` (not `cdp_interceptor`). The CLI is `uv run cdp-spy` (still `python -m common.cdp_interceptor.spy` if you prefer). Public API is unchanged; `find_chrome` / `ChromeNotFoundError` are backwards-compat aliases for the OS-adaptive `find_browser` / `BrowserNotFoundError`.
